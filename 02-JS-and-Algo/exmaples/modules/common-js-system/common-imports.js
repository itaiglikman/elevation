const math = require('./common-exports'); //import all under one thing
const {add} = require('./common-exports'); //import specific item (depends how waw exported)
const {PI} = require('./common-exports');

math.add();
add();